<h1>Articles</h1>
<h1>Our blog has <%= Article.public_count %> articles and counting!</h1>
<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
    <li>
      <%= link_to(article.title, article) %>
    </li>
    <% end %>
  <% end %>

  <button type="button" style="border-radius : 5px"> <a href=<%=new_article_path %> >  NewArticle </a> </button>
</ul>
